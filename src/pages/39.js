import React from 'react';
import Link from 'gatsby-link';
import Container from '../components/Container';

export default ({ transition }) => (
  <div style={transition && transition.style}>
    <Container>
      In the current automation building market, builder tools are very limited in how they can deliver the automations to those who consume them. Even when the tool has a marketplace, it’s often a pain to set up and confusing for the user in knowing what they are doing and why. Infusionsoft’s new building tool not only delivers more power, more extensions, and a better experience, but also has a unique packaging solution for end-users to use without going into the tool itself.
    </Container>
  </div>
);
