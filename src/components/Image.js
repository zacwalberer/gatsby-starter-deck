import React from 'react';
import Stats from '../images/stats.png';

export default ({ children }) => (
  <div style={{ display: 'flex', justifyContent: 'space-around', height: '65vh' }}>{children}</div>
);
